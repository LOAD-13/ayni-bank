# Versiones fijadas. Al subirlas, verificar que Trivy no reporte HIGH ni CRITICAL.
fastapi==0.141.1
starlette==1.6.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2
# CVE-2024-53981, CVE-2026-24486, CVE-2026-42561, CVE-2026-53539: corregidos en 0.0.30+
python-multipart==0.0.32
minio==7.2.9
# 8.x es la primera serie compatible con starlette 1.x
prometheus-fastapi-instrumentator==8.1.0
# headless: sin dependencias de GUI/X11, correcto para un contenedor servidor.
# Se fija en la serie 4.x (no 5.x) para evitar cambios de API mayores.
opencv-python-headless==4.14.0.94
# opencv-python-headless exige numpy>=2. Se fija 2.0.2 (no una serie mas
# reciente): numpy>=2.1 rompe la resolucion de NDArray en mypy 1.11.2, que es
# la version de mypy fijada en requirements-dev.txt. Al subir mypy, revisar
# si esta version de numpy ya puede subir tambien.
numpy==2.0.2
