# Versiones fijadas. Al subirlas, verificar que Trivy no reporte HIGH ni CRITICAL.
fastapi==0.141.1
starlette==1.6.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2
# CVE-2024-53981, CVE-2026-24486, CVE-2026-42561, CVE-2026-53539: corregidos en 0.0.30+
python-multipart==0.0.32
minio==7.2.9
# 8.x es la primera serie compatible con starlette 1.x
prometheus-fastapi-instrumentator==8.1.0
# paddlex[ocr-core] (dependencia de paddleocr) fija opencv-contrib-python
# EXACTAMENTE en 4.10.0.84 (con "=="), sin margen de version. No se puede usar
# opencv-python-headless aqui: paddlex instala su propio opencv-contrib-python
# de todas formas, y tener dos paquetes de opencv distintos deja cual "gana"
# al azar segun el orden de instalacion (ver ADR-0014). No es headless, pero
# nuestro codigo (subtareas 3-4) es compatible sin cambios: se verifico
# manualmente contra esta version exacta antes de fijarla.
opencv-contrib-python==4.10.0.84
# numpy==2.0.2, no una serie mas reciente: numpy>=2.1 rompe la resolucion de
# NDArray en mypy 1.11.2, que es la version de mypy fijada en
# requirements-dev.txt. Al subir mypy, revisar si esta version de numpy ya
# puede subir tambien.
numpy==2.0.2
# OCR de anverso y reverso del DNI (subtarea 5 de AYNI-13). paddlepaddle es el
# framework de deep learning que paddleocr usa como backend.
paddlepaddle==3.3.1
paddleocr==3.7.0
deepface==0.0.92
# deepface no fija tensorflow, asi que sin este pin arrastra el mas reciente
# (2.21.0), cuyo tf.keras usa Keras 3 por defecto. mtcnn (dependencia de
# deepface, importada siempre al cargar DeepFace sin importar el
# detector_backend elegido) todavia usa la API de Keras 2 (`tf.keras.layers`)
# y revienta con ImportError al arrancar. tf-keras reinstala el shim de
# Keras 2 que tf.keras usa si esta presente; version igual a la de
# tensorflow, que es como Keras versiona la compatibilidad entre ambos.
tensorflow==2.21.0
tf-keras==2.21.0
